- name: Deploy App
  hosts: web
  tasks:
    - name: Install Docker
      apt:
        name: docker.io
        state: present
        update_cache: yes

    - name: Start Docker
      service:
        name: docker
        state: started

    - name: Run container
      shell: docker run -d -p 3000:3000 myapp
